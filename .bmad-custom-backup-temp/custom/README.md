# SafeDrop Custom BMAD Agents

This folder contains the SafeDrop agent definitions and shared prompts copied from `.agent/agents` and `.agent/prompts`, registered in `.bmad/_cfg/agent-manifest.csv` under the `custom` module so BMAD/party-mode can see them.
